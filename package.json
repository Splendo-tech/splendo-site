{
  "name": "splendo-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "sync-partials": "node scripts/build.js",
    "test": "node scripts/check-tags.js && node scripts/check-i18n.js && node scripts/build.js --check",
    "hooks:install": "bash scripts/install-hooks.sh"
  },
  "dependencies": {
    "stripe": "^22.6.1"
  }
}
